PHP framework for audio synthesis.

"Oväsen" is swedish for "noise". o- is the standard swedish negative prefix,
and "väsen" actually means "fiend", so there's a riddle for you...

github can't HANDLE the truth so the repo is called "ovasen". Sorry 'bout that.